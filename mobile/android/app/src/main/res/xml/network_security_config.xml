<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security config:
  - Production traffic: HTTPS only
  - Emulator localhost (10.0.2.2, localhost): HTTP allowed for Metro + local backend
  - debug-overrides: trust user-installed certs for Charles/Proxyman debugging
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow HTTP to emulator localhost for Metro bundler and local backend -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- Trust user certs in debug builds (useful for proxy tools) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
